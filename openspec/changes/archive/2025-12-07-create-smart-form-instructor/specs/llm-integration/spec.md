## ADDED Requirements
### Requirement: LLM Abstraction
The system SHALL provide an abstraction layer for LLM providers to allow swapping models.

#### Scenario: Model Switch
- **WHEN** configured to use a different LLM provider
- **THEN** the extraction logic remains consistent without code changes in the core logic.


